import type { Metadata } from "next";
import { Manrope, Space_Grotesk } from "next/font/google";
import "./globals.css";

const manrope = Manrope({
  variable: "--font-manrope",
  subsets: ["latin"],
});

const spaceGrotesk = Space_Grotesk({
  variable: "--font-space-grotesk",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Tepnology | Contract Opportunity Intelligence",
  description:
    "Tepnology turns SAM.gov and local procurement data into focused opportunity reports for transportation, parking, and janitorial contracts.",
  icons: {
    icon: "/tepnology-icon-32x32.png",
    shortcut: "/tepnology-icon-32x32.png",
    apple: "/tepnology-icon-32x32.png",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${manrope.variable} ${spaceGrotesk.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
